# 一键部署（Deploy to Cloudflare 按钮）时，部署配置页只要求填写下面两个必填项。
# 可选功能（AI 总结、Giscus 评论）默认不启用，需要时部署后设置：
#   wrangler secret put OPENAI_API_KEY      # 启用 AI 总结
#   wrangler secret put GISCUS_REPO_ID      # 启用 Giscus 评论（还有 GISCUS_CATEGORY / GISCUS_CATEGORY_ID 等）
# 详见 README「一键部署」小节。

# 必填：博客后台管理员账号（登录 /admin 用）
ADMIN_USER=admin
ADMIN_PASS=change-me-to-a-strong-password
